# Inventory Rules

INVT_MASTER is fixed. Distributor warehouse comes from distributor configuration. Normalize configured leading-zero SKUs. Apply distributor multiplier rules at the legacy-defined stage before final comparison. Stock Excel defaults: SKU index 20; quantity `StokAkhir`, fallback index 71. UI may override columns.
