# Migration Rules

Legacy repo is reference-only. Inspect before changing behavior. Refactor `playwright_engine.py`, `data_processor.py`, and `error_taxonomy.py` into testable domain/services instead of blindly copying them. Remove Streamlit dependencies from the target system.
