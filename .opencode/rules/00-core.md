# Core Rules

Read `AGENTS.md` first.

Never add Streamlit or Celery. Never expose secrets. Prefer small vertical slices with tests. Preserve legacy business behavior until it is explicitly changed and documented.
